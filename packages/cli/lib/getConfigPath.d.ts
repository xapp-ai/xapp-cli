/**
 * Returns the path to the config object, it will create the root directory
 * if it doesn't exist.
 *
 * @param name - Path to the config object
 */
export declare function getConfigPath(name?: string): string;
